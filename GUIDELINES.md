# 運用指針

思考・感情・行動計画を記録し、定期的に振り返ることで自己理解を深め、行動につなげるためのリポジトリ。

## 全体構成

```
life/
├── README.md
├── GUIDELINES.md    本ファイル
├── notes/
│   ├── events/       単発の出来事ベースの記録 (YYYY-MM-DD-スラッグ.md)
│   └── themes/       継続して向き合っているテーマの記録 (スラッグ.md、随時追記)
├── reviews/          定期レビューの記録 (YYYY-MM-DD-review.md)
└── templates/        各種テンプレート
```

## 記録の使い分け

- **events**: 「今日〇〇があった」という単発の出来事。ファイル名は `YYYY-MM-DD-短い説明.md`。
- **themes**: 「転職」「人間関係」など継続して向き合っているテーマ。ファイル名は `短い説明.md` で、1ファイルに日付区切りで追記していく。
- 迷ったら、まず events に書く。同じ話題が3回以上出てきたら themes に格上げ(統合)する。

## ノートのフォーマット

簡易メタデータ(frontmatter) + 自由記述。

```yaml
---
type: event | theme
created: YYYY-MM-DD
updated: YYYY-MM-DD
mood: 0        # -5(最悪)〜+5(最高) の主観スコア
tags: []
---
```

本文は思考と感情を分けず、自由に書いてよい。フォーマットの厳密さより書き続けられることを優先する。

## 行動計画とIssue運用

- ノートを書いている最中、または見返している最中に「やるべきこと」に気づいたら、その場でこのリポジトリに GitHub Issue を立てる。
- 行動計画そのものをMarkdownファイルでは管理しない。Issueに一元化することで、進捗管理(open/close)やラベルでの優先度付けができる。
- Issueのタイトル: やることを簡潔に。本文には、きっかけになったノートへのパスを書く(例: `from: notes/events/2026-09-10-〇〇.md`)。
- ノート側にもIssue番号を追記して相互参照する(例: `-> #12`)。
- ラベル例: `priority:high` / `priority:mid` / `priority:low`、`area:仕事`、`area:健康` など。運用しながら増やしてよい。

## 定期レビュー

- 頻度: 週1回を基本とする(合わなければ調整してよい)。
- レビューファイル: `reviews/YYYY-MM-DD-review.md`
- レビューでやること:
  1. その期間の notes(events/themes)を読み返す
  2. mood の推移や頻出タグから傾向を把握する
  3. 新たに気づいたタスクをIssue化する
  4. 既存Issueの優先度を見直す・不要なものはクローズする
  5. 次回レビューまでの重点テーマを1〜3個決める

## 命名・タグの緩やかなルール

- ファイル名はスラッグ(日本語可、スペースは `-` に置換)。
- タグは最初から統一しすぎず、運用しながら育てる。半年に一度くらい整理する。

## プライバシー

非公開前提のリポジトリとして運用する。特別な匿名化・伏せ字対応は行わない。

## 運用の心構え

- 完璧なフォーマットより「書き続けられること」を優先する。
- ルールに迷ったら、まず書く。整理は定期レビューでまとめて行う。
